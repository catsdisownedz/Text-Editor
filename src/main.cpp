#include <iostream>
#include "Editor.h"
#include "GUI.h"

using namespace std;

int main() {
    
    cout << "Starting the Text Editor..." << endl;

    // Initialize the GUI
    //GUI gui;

    // Start the application loop
    //gui.run();

    return 0;
}
